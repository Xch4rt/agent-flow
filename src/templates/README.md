# Templates

MVP templates currently live as TypeScript functions in `src/core/templates.ts` and `src/adapters/codex/templates.ts`.

Keep this directory for future file-backed templates if the generated content grows large enough to justify moving it out of code.
